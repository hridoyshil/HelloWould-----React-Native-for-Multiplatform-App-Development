import * as actionTypes from './actionTypes';

const initState = {
}

export const rootReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}